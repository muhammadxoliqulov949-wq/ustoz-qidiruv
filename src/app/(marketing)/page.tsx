import { Hero } from "@/components/home/hero";

/**
 * USTOZ homepage — Phase 1 scope:
 * Header (global layout) + Hero + Hero Search + Quick Filters.
 * Trust strip, Categories, Featured courses, Teachers and Footer arrive
 * in Phase 2 as additional sections in this file.
 */
export default function HomePage() {
  return (
    <>
      <Hero />
    </>
  );
}
